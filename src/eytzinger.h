#ifndef EYTZINGER_H
#define EYTZINGER_H

template<typename T>
class ESet{
  private:
    T *data;
  public:
    ESet();
    bool contains(T data);
};

template<typename T>
class EMap{
  
};

template<typename T>
class EMapDense{
  
};

#endif
